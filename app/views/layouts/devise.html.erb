<!DOCTYPE html>
<html lang="da">
  <%= render 'layouts/head' %>
  <body class="bg-gray-200 font-sans antialiased min-h-screen flex flex-col">
    <div class="container max-w-sm mx-auto flex-1 flex flex-col items-center justify-center px-2">
      <div class="mb-6 text-3xl text-center">
        <%= render 'shared/logo' %>
      </div>
      <%= yield %>
    </div>
  </body>
</html>
